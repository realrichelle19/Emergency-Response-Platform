# Render Deployment Guide

## 🚀 Deploy to Render (Free Tier)

Your Emergency Response Platform is ready for Render deployment! Here's how to deploy it:

### Prerequisites
- GitHub account with your code pushed
- Render account (free tier works)

### Deployment Steps

1. **Push to GitHub**
   ```bash
   git add .
   git commit -m "Prepare for Render deployment"
   git push origin main
   ```

2. **Connect to Render**
   - Go to [render.com](https://render.com)
   - Sign up/login with GitHub
   - Click "New +" → "Web Service"
   - Connect your repository

3. **Configure Service**
   - **Name**: `emergency-response-platform`
   - **Environment**: `Python 3`
   - **Build Command**: `cd backend && pip install -r requirements.txt`
   - **Start Command**: `cd backend && python run.py`
   - **Plan**: Free

4. **Environment Variables**
   Add these in Render dashboard:
   ```
   FLASK_ENV=production
   FLASK_CONFIG=production
   SECRET_KEY=<auto-generated by Render>
   DATABASE_URL=sqlite:///emergency_response.db
   ```

5. **Deploy**
   - Click "Create Web Service"
   - Wait for deployment (5-10 minutes)
   - Your app will be live at `https://your-app-name.onrender.com`

### ✅ What's Included

- **Auto-initialization**: Database tables and sample data created on first run
- **Sample users**: Admin, authority, and volunteer accounts ready to use
- **Static files**: Frontend served through Flask
- **CORS configured**: API accessible from frontend

### 🔑 Default Login Credentials

After deployment, use these accounts:

**Admin**
- Email: `admin@emergency.local`
- Password: `password123`

**Authority (Fire Chief)**
- Email: `fire.chief@emergency.local`
- Password: `password123`

**Volunteer**
- Email: `volunteer1@emergency.local`
- Password: `password123`

### ⚠️ Free Tier Limitations

- **Sleep mode**: App sleeps after 15 minutes of inactivity
- **Cold starts**: First request after sleep takes ~30 seconds
- **SQLite**: Data persists but limited to single instance
- **750 hours/month**: Free tier limit (enough for personal projects)

### 🔧 Troubleshooting

**App won't start?**
- Check build logs in Render dashboard
- Ensure all dependencies in requirements.txt
- Verify Python version compatibility

**Database issues?**
- SQLite file is created automatically
- Sample data loads on first run
- Check logs for initialization errors

**Frontend not loading?**
- Static files served from `/frontend` path
- Check CORS configuration in app/__init__.py
- Verify template paths in Flask config

### 🚀 Going Production

For production use, consider:
- **PostgreSQL**: Render's managed database
- **Custom domain**: Add your own domain
- **Environment secrets**: Use Render's secret management
- **Monitoring**: Set up health checks and alerts

### 📝 Next Steps

1. Test all functionality after deployment
2. Update frontend API URLs if needed
3. Configure custom domain (optional)
4. Set up monitoring and backups
5. Update documentation with live URL

Your Emergency Response Platform is now live and ready to coordinate emergency responses! 🚨